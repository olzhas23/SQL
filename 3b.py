import sqlite3
import random

conn=sqlite3.connect ("newnum.db")

cursor=conn.cursor()
prompt=""" enter number"""

while True:
	x=raw.input(prompt)
	if x in set (["1", "3", "4"]):
	
# project is not done yet!
	
	